# imageCompress — 简单的图片压缩服务

基于 [Gin](https://gin-gonic.com/) 的图片压缩 HTTP 服务:通过接口上传一张图片,服务对它重新编码压缩(可选等比缩小),并把压缩结果直接放在响应体里返回。**全程在内存中处理,不会把任何文件写到磁盘。**

## 功能特性

- 支持 **PNG / GIF / JPEG** 三种格式(按文件真实内容识别,不看扩展名)
- PNG 以最高压缩率(`BestCompression`)重新编码;JPEG 按 `quality` 参数压缩
- 可选 `scale` 参数等比缩小图片(使用 CatmullRom 高质量缩放算法)
- **令牌桶限速**:平均约 3 请求/秒,突发上限 1,超限返回 429;被拒请求会被直接中止(`c.Abort()`),不消耗压缩资源
- **防解压炸弹**:完整解码前先用 `image.DecodeConfig` 读头部尺寸,宽或高超过 4096 的图片直接拒绝,不会被分配巨量内存
- 读取上限 8MB,防止超大请求体拖垮服务
- 响应的 `Content-Type` 由实际解码出的格式决定,不信任客户端声明

## 快速开始

环境要求:Go 1.26+(见 `go.mod`)。

```bash
# 启动服务(监听 :46939)
go run .
```

看到 gin 的启动日志后,服务就绪(限速器预置了初始令牌,第一个请求可立即处理)。

## API 说明

### POST /compress

**请求**:`multipart/form-data`,文件字段名为 `image`。

**查询参数**:

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `quality` | 否 | 压缩质量,1–100 的整数,**缺省默认 100**。只有 JPEG 的编码真正受它影响;PNG/GIF 忽略该值(但传了仍必须是 1–100 的整数,否则 400) |
| `scale` | 否 | 缩放比例,取值 `0 < scale ≤ 1`,如 `0.5` 表示宽高各缩一半;不传则不缩放。非法值(0、负数、大于 1、非数字、`NaN`、`Inf`)返回 400 |

**响应**:

| 状态码 | 内容 |
| --- | --- |
| 200 | 压缩后的图片字节流,`Content-Type` 为 `image/png` / `image/gif` / `image/jpeg` |
| 400 | JSON:`{"error": "原因"}`(缺少文件、参数非法、图片损坏、尺寸超过 4096×4096 等) |
| 429 | JSON:`{"error":"Too many requests."}`;请求被限速器中止,不会执行压缩逻辑 |

**curl 示例**:

```bash
# 压缩一张 PNG(不传 quality,按默认 100;结果存为 out.png)
curl "http://127.0.0.1:46939/compress" -F "image=@photo.png" -o out.png

# 缩小到一半再压缩
curl "http://127.0.0.1:46939/compress?scale=0.5&quality=80" -F "image=@photo.png" -o out.png

# 压缩 JPEG(quality 对 JPEG 真正生效)
curl "http://127.0.0.1:46939/compress?quality=80" -F "image=@photo.jpg" -o out.jpg
```

提示:限速为平均 3 请求/秒,连续快速调用会收到 429,稍等约 0.4 秒重试即可。

**格式行为说明**:

- 输入 PNG → 输出 PNG;输入 GIF → 输出 GIF;输入 JPEG → 输出 JPEG
- **GIF 只保留第一帧**,动图压缩后会变成静态图
- 其他格式(BMP、WebP 等)未注册解码器,会被当作无效图片拒绝(400)

## 测试

测试位于 `main_test.go`,通过 `httptest` 直接调用 handler 和中间件,不需要真的启动服务:

```bash
go test ./...        # 运行全部测试
go test -v ./...     # 查看每个用例的详细结果
```

覆盖场景:

- PNG / GIF / JPEG 的压缩与 `scale` 缩放、`quality` 边界值(0、101、非整数、缺参走默认 100)
- 非法 `scale`(0、负数、大于 1、非数字、`NaN`、`Inf`)均返回 400
- 缺少文件字段、损坏图片(不再 panic)
- 防炸弹:手工构造"声明 5000×5000"的合法 PNG 头 → 被尺寸校验拒绝;4096×4096 边界放行
- 限速中间件:无令牌 429(响应体是恰好等于的纯 JSON)、有令牌放行、突发上限 1、初始令牌预置、`tokenInit` 自动补充令牌

## 已知问题(待修复)

此前报告并修复的问题——#1(quality 参数误读 scale)、#2(Decode 错误未检查导致 panic)、#3(err 被遮蔽)、#4(NaN 绕过校验)、#5(429 分支缺 `c.Abort()`)、#6(启动无初始令牌),以及两个遗留项(`quality := 100` 默认值不生效、图片炸弹无防护)——**均已修复并通过测试验证,当前没有已知的代码问题**。

剩余的是[安全说明](#安全说明)"尚未处理"里列出的部署层面事项(鉴权、TLS、日志注入),不属于功能 bug。

## 安全说明

**已有的防护**:

- `io.LimitReader` 限制最多读取 8MB,`MaxMultipartMemory` 限制 16MB
- **防解压炸弹**:`image.DecodeConfig` 在完整解码前拦截声明尺寸宽/高 > 4096 的图片(8MB 的小文件解出几 GB 内存的攻击被封死),返回 400 `Image is too large.`
- 输出格式由真实解码结果决定,客户端无法通过伪造 Content-Type/扩展名让服务返回错误类型
- 不写文件到磁盘,不存在路径穿越问题
- 损坏/非图片内容返回 400,不再触发 panic(#2 已修复)
- 令牌桶限速限制了请求频率,超频请求被 `c.Abort()` 直接中止,不执行压缩(#5 已修复)

**尚未处理的风险**(部署到非本机环境前请先解决):

- **限速 ≠ 鉴权**:当前任何人不需要凭证就能调用接口,限速只防"刷得太快",不防"陌生人使用"。`Run(":46939")` 绑定所有网卡(0.0.0.0),公网服务器上对全网开放;且无 TLS,传输内容明文。只在本机使用可改为 `Run("127.0.0.1:46939")`;要对外服务应加上真正的身份验证(如 API Key / Token 校验中间件)、HTTPS 反向代理。
- **日志注入**:上传的文件名未经处理直接 `log.Println`,文件名里带换行符可以伪造日志行。

## 项目结构

```
.
├── main.go        # 服务入口、限速中间件与压缩逻辑
├── main_test.go   # handler 与中间件测试(httptest,无需启动真实服务)
├── go.mod / go.sum
└── README.md
```
